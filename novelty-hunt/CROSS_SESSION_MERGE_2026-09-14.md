# Cross-Session Merge — Canonical Estate Forensics + Novelty Hunter

**Merged:** 2026-09-14  
**Purpose:** preserve the separate September 13–14 estate-forensics session inside the durable novelty-hunt state so future sessions do not fork history.

This file is a **merge layer**, not a replacement for `CANONICAL_LEDGER.md`. It records material present in the other canonical session that must remain visible to every future novelty sweep.

## Source authority recovered from the other session

The other session had already canonicalized its work into exactly two master artifacts:

- `MATH_ESTATE_CANONICAL_LEDGER_2026-09-13.xlsx`
- `MATH_ESTATE_CANONICAL_FORENSIC_REPORT_2026-09-13.md`

A later Library copy of the forensic report reaches **14,826 lines / Round 40**. Earlier per-sweep reports were explicitly superseded rather than independently extended.

The key cross-session doctrine is compatible with Novelty Hunter:

> **A result loses novelty, not mathematical existence.**

and

> **Closure is too narrow an accounting unit.**

The estate must preserve exact classifications, theorem suites, cross-problem fusions, repaired theorems, engine-born mathematics, method obstructions, publication gaps, and independent rediscoveries even when no famous parent problem is fully closed.

---

# A. Closure court imported from the other session

## Erdős #655 — full literal close, independent rediscovery

Regular `n`-gons determine exactly `floor(n/2)` distinct distances, so the literal frozen statement cannot force `(1+c)n/2` distances for fixed `c>0`.

**Court:**
- mathematical truth survives;
- literal frozen parent statement is negatively closed;
- novelty does **not** survive: Zach Hunter publicly recorded the same regular-polygon observation;
- keep a compact historical independent-rediscovery note rather than deleting the theorem.

The other-session court found **no second full campaign-generated Erdős close** in its audited September layer.

---

# B. Drained search surfaces — do not repeat blindly

## 617 latest-local `PROVED` states

The other session exhaustively drained the obvious registry surface as a **headline/scope search**, not as 617 automatically certified theorems:

- **617/617** latest-local `PROVED` states reviewed;
- **282/282** omitted-family states across **97 problem families**;
- **335/335** represented-family states checked for stronger versions;
- **0** additional full Erdős closes.

This closes the naive hypothesis that another giant headline is simply sitting in an unreviewed `PROVED` row.

Important surviving corrections from that drain:

- **#477:** the estate theorem is not merely the square case; it covers **every quadratic integer polynomial** `f(k)=ak^2+bk+c`, `a != 0`: no unique direct-sum complement exists.
- **#849:** exact representation-multiplicity endpoints were already present through `t=4`: `120` for `t=3`, `3003` for `t=4`; these are known public mathematics, not novelty claims.
- **#126:** no three distinct positive integers have all three pairwise sums powers of two; valid compact child theorem.
- **#1107:** exact finite certificate: `87` is not a sum of one, two, or three positive powerful numbers.

Permanent regression cases from this surface include #891 semantic-variant confusion, #943 additive-vs-multiplicative operation mismatch, and #701 finite theorem versus false universal-star upgrade.

## Pass-4 formalizer residue

The separate forensic run also isolated **556 math-bearing formalizer objects** across **76 problem families**:

- 204 distinct route/lemma targets;
- 464 `KERNEL_DECIDABLE` objects;
- 90 `LEAN_PROOF` objects;
- 2 `WITNESS_FINITE` objects;
- 544 `NO_TYPED_MOVE`;
- 12 `KERNEL_REJECTED`.

Formalizer failure was explicitly treated as neither proof nor refutation. This residue has already been mined as a separate authority surface and should not be restarted from zero without a new lens.

---

# C. High-value cross-session results that must remain on the novelty radar

## C(13,6,3)

Frontier:

`20 <= C(13,6,3) <= 21`.

The estate has two independently validated 21-block covers and severe structure for any hypothetical 20-cover: pair multiplicities, point-degree restrictions, excess budgets, surviving degree multisets, local triple constraints, and near-miss basins.

The Lean point-degree theorem is actually stronger than its campaign name: **any** 6-uniform triple-cover on 13 points has every point in at least 8 blocks; the nominal 20-block hypothesis is unused.

## Erdős #973

Exact `n=2` minimax value:

`(sqrt(5)-1)/2`.

The parent was later solved negatively elsewhere; this exact golden-ratio finite extremum remains a standalone mathematical result.

## Erdős #1093 — LCM divisor-window theorem

For admissible `(n,k)`, with

`L_k = lcm(1,...,k)`,

deficiency is exactly

`delta(n,k) = #{ d | L_k : n-k < d <= n }`.

Thus a smooth-number problem becomes finite divisor geometry for fixed `k`. An exact engine through `k<=45` reproduces all 16 listed deficiency-`>1` examples in the audited range, including `delta(284,28)=9`.

This feeds directly into the already-promoted **#890 ↔ #1093 deficiency/excess bridge**.

## Erdős #289 — all-prime p-adic reciprocal-sum obstruction

The deeper court recovered a formulation stronger than the familiar `p=2` parity/Kürschák presentation: if a finite reciprocal set has integer sum, **every prime** imposes a valuation condition on the subfamily divisible by that prime.

Status: formalization + literature collision needed before novelty language.

## Erdős #885 — factor-difference / square duality

The exact identity

`d in D(N)  iff  exists s >= 0 with s^2 = d^2 + 4N`

turns common factor-difference sets into simultaneous-square / integral-point systems.

A sorry-free Lean theorem exists in the public theorem bank. Treat this as a structural coordinate change, not a parent close.

## Erdős #598 — exact finite polychromatic values

The other session recovered exact finite values:

- `L(4,3,2)=2`, maximum colors `3`;
- `L(5,3,2)=4`, maximum colors `2`;
- `L(6,4,2)=3`, maximum colors `5`.

Extremizers are respectively tied to `K4` matchings, `C5` / complement, and a `K6` one-factorization.

Keep as finite exact combinatorics; specialist novelty status still needs adjudication.

## Erdős #238

Do **not** promote from the old consolidated summary without re-freezing the source statement. One layer gave a strong `c_2<2` slice, while route-level audits warned about a `k_max` / transfer mismatch. A restricted small-`c_1` theorem is classical (Erdős 1949), and an exact finite window-cover lemma survives with the bad-gap window count sharpened from `k` to `k-1`.

Status: statement reconciliation / known-prior child theorem, not a headline.

---

# D. Fiber coherence / rank-three program — corrected novelty boundary

Canonical public home:

`jaredwilder/fiber-coherence-cycle-rank`

The program has a large replay surface (the other session records **66,166 finite assertions**), but the novelty court separates classical topology from source-specific mathematics.

## Classical / infrastructure

After suppressing degree-two vertices, loopless 2-connected cyclomatic-rank-three kernels fall into exactly four topologies:

`Q4, T221, D22, K4`.

That pure topology classification lies in classical reduced-graph / cyclomatic-number territory and should **not** be claimed as new.

Generic fixed-feedback-set / fixed-cycle-rank tractability is likewise not the main novelty.

## Source-specific relation semantics

The four rank-three kernel topologies acquire exact relation mechanisms:

- `Q4`: four-way relation intersection;
- `T221`: parallel intersections then ternary join;
- `D22`: parallel intersections then cycle monodromy / fixed-point test;
- `K4`: exact four-variable binary CSP on six kernel edges.

Every tree-absorbed 2-connected rank-three fiber block uses one of these four relation mechanisms.

## Strongest distinctive theorem pair

The other session identifies the most distinctive part as:

`arbitrary binary relation -> K4-free exact strip -> arbitrary binary CSP exact K4-free realization`.

This theorem pair deserves a hostile prior-art court.

## First unclassified finite wall

For the finite-fiber #595 compression, any minimal incoherence core either:

1. has cycle rank at most three and is handled by the existing forest/unicyclic/theta-cactus/rank-three algorithms;
2. has cycle rank at least four; or
3. every finite subsystem coheres, where compactness supplies a full coherent section and a Berge-`C3`-free hypergraph compression.

Therefore **cycle rank four is the first fixed-rank finite core not exhaustively classified**.

This belongs on the future-theory queue, while #595 itself remains open.

---

# E. Erdős #738 — exact theorem-bank result imported from Round 40

The old label “missing 62-theorem package” is obsolete. The focused public home now exists:

`jaredwilder/erdos738-triangle-free-induced-trees`

Core source:

- **62** `PROVED_IN_PACKET` statements;
- **12** `UNPROVED_CHECKABLE_TARGET` records;
- public research surface expanded to **174 claims** with status separation.

## Triangle-free chromatic-budget calculus

If `G` is triangle-free and `G[S]` has no isolated vertices,

`chi(G[N[S]]) <= |S|`,

hence

`chi(G - N[S]) >= chi(G) - |S|`.

Sequential protected deletions telescope to subtract the total protected-set budget.

Useful calculus; mostly elementary/classical-adjacent rather than dozens of novelty claims.

## Path-contact code

Contacts of an outside vertex on an induced path form a binary subset with no consecutive positions, so the number of contact signatures on an `n`-vertex path is at most `F_{n+2}`. More general separation `d` obeys

`a_d(n)=a_d(n-1)+a_d(n-d)`.

Again: exact scaffolding, not the top novelty layer.

## Mixing/spider dictionary

The mixing number at a vertex equals the maximum number of arms in an induced once-subdivided star centered there. Prescribed connected-region reach profiles correspond exactly to induced spider arm lengths. The recursive shielding criterion gives an induced rooted-tree extension calculus.

## Strongest distinctive theorem family: T06–T09

For a complete ordered `d`-ary rooted tree (`d>=3`) embedded path-induced, level-stable, and type-uniform in a triangle-free host, fix join depth `c` and define the ordered depth-pair adjacency support `A_c`.

Triangle-freeness and parent-child constraints make each support slice an independent set in the depth-pair grid. Writing `n=k-c`, the sharp bound is

`|supp(A_c)| <= floor(n^2/2)`.

Across all join depths:

`# active ordered types <= sum_{n=1}^k floor(n^2/2)`.

Closed forms:

- `k=2m`: `m(m+1)(4m-1)/3`;
- `k=2m+1`: `m(m+1)(4m+5)/3`.

### Parity-defect extremizer

Take the underlying rooted tree, keep tree edges, and connect incomparable vertices iff their depths have opposite parity. The host remains triangle-free and attains

`|supp(A_c)| = floor(n^2/2)`

in every slice.

Thus the T06 bound is sharp.

### Exact induced-subtree criterion in the extremizer

A selected rooted subtree is induced **iff every pair of incomparable selected vertices has the same depth parity**.

### Novelty status

Targeted searching found the general type-uniform Gyárfás–Sumner framework but not the exact sharp `floor(n^2/2)` support theorem + parity-defect extremizer + equality package.

Other-session grade: **N2 candidate — no direct match found, priority not established**.

This is now a serious Novelty Hunter target.

---

# F. RH / Weil auxiliary mathematics — preserve without turning it into an RH claim

The separate forensic program identified a substantial non-Erdős auxiliary-math package around the Weil quadratic-functional framework.

**Hard boundary:** RH remains open; no off-critical zeta zero was found.

## Exact isolated-quartet spectral core

For

`K_a(u,v)=2(cosh(a(u-v))-1)`

on `[-L,L]`, `sinh(au)` is an exact odd eigenfunction with eigenvalue

`-(sinh(2aL)/a - 2L)`.

Finite clusters reduce to a compact signed/J-Gram model; under the relevant independence assumptions, `m` off-line quartets yield `m` negative directions.

## Repaired compact-jet invisibility theorem

The valid polynomial deformation

`q_{a,Y}(w)=((w^2-(a+iY)^2)(w^2-(a-iY)^2))/(a^2+Y^2)^2`

is entire, even, real on the real axis, normalized at zero, inserts the symmetric quartet `±(a±iY)`, and converges to `1` on every fixed compact set in every fixed finite derivative order as `Y -> infinity`.

Therefore a certificate depending only continuously on finitely many compact analytic data / finite jets plus generic evenness, reality, and order-one growth cannot by itself exclude all sufficiently high inserted off-line quartets.

This is a **no-go for a named information model**, not a statement that RH cannot have a finite proof.

## No uniform cluster-floor theorem

For fixed `a,L,r>0`, in the unrestricted finite partner-quartet family,

`inf_Q |lambda_min(Q)| / |lambda_iso(a,L)| = 0`,

including `aL>=4`. The infimum remains zero with distinct unit-multiplicity partner parameters if arbitrarily close parameters are permitted.

So the meaningful quantity is a complexity/separation-dependent floor `delta_m(...)`, not a universal positive constant.

## Live post-report additions from the other session

The other session continued beyond the Round-40 forensic report. Preserve these as **live research state pending the same novelty court**:

### Epoch 40 adaptive theorem

Under the session's RH-failure dominant-zero setup:

- `a_c -> A`;
- `|gamma_c|/c -> 0`;
- `c g_c -> 2A`;
- at `L=(1+epsilon) log log c / (2A)`, the legal background ratio is bounded by
  `<< log log c * (log c)^(-epsilon+o(1)) -> 0`.

Published commit recorded in the other session:

`985f71d45f5c9d918f03abcbe652770741ea60d7`.

A generic score-shell masking counterexample/no-go was also published:

`330ac299983ab8b69c41a153e36904f2783ee3cc`.

### Epoch 41 Blaschke separation

The session derived the center-independent separation product

`Delta(u0)=prod_{v != u0} |(u0-v)/(u0+conj(v))|`.

Published commit:

`733fa20d5bcd2e2c4136c2cad2afd35d32cad528`.

The remaining stated obstruction is finite-degree two-sided quartet interpolation together with moving separation / PSD retention.

These live Epoch 40/41 results are **not yet promoted into the main Novelty Hunter board**. They are preserved so a future session can adjudicate them instead of rediscovering them.

---

# G. Publication debt / authority debt imported from the other session

The older forensic report's publication debt must be reconciled with subsequent release work, not blindly copied.

Already resolved by later rounds:

- #738 theorem bank now has a focused public home.

Still important as preservation checks:

- central RH method-obstruction files need to remain visible in the RH subject home;
- local registry statuses are provenance, not theorem authority;
- formal artifacts must be judged by the actual theorem statement and axiom footprint;
- the complete historical EG203 archive should preserve unconditional / conditional / circular-deprecated layers separately;
- #655 should retain a historical rediscovery surface even though novelty was lost.

---

# H. Permanent quarantine / regression set

Do not allow future mining to resurrect these as closes without re-proving the broken edge:

- #539 false identity;
- #101 invalid witness (five-collinear violation);
- #193 target-version / infinite-range mismatch;
- #410 divisor-sum semantics confusion;
- #510 zero admitted by weakened domain;
- #829 Mahler-style close with no valid branch-B witness;
- #1203 later explicit value refutes universal claim;
- #1210 shifted coprimality step false;
- #683 finite contract closed but global extrapolation unproved;
- #891 `Omega`-with-multiplicity theorem does not solve the live distinct-prime-factor `omega` problem;
- #943 multiplicative factorization substituted for additive convolution;
- #701 finite Chvátal-star results survive, but the universal “every intersecting hereditary family lies in one star” claim is false.

Negative/corrective results are preserved as **regression tests**, not erased.

---

# I. Cross-session consequences for the live novelty board

The other session changes Novelty Hunter in four concrete ways:

1. **#738 T06–T09 must be added to the active novelty queue.** Its sharp `floor(n^2/2)` type-support bound plus parity-defect extremizer is stronger than the generic “62-theorem bank” summary.
2. **Fiber coherence must be hunted at the relation/CSP layer, not the classical rank-three topology layer.** The strongest target is exact K4-free realization of arbitrary binary relations/CSPs.
3. **#973, #289, #885, #598, and the all-quadratic #477 scope correction are preserved as real mathematical assets** even though not all belong in the top publication queue yet.
4. **RH Epoch 32/40/41 auxiliary mathematics remains a separate novelty court** with an explicit no-RH-claim boundary.

The master state remains:

`SATURATION = false`.
