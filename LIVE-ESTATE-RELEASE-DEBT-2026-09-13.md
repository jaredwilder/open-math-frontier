# Live estate release-debt ledger — 2026-09-13

**Author / estate owner:** Jared Wilder

This file reconciles the September 11–13 release-session reports against the **current public GitHub state**. It exists because a session-end report can become stale within hours as another release session promotes the missing object.

## Rule

A debt is closed only when its **actual mathematical/source object** has a public destination with the correct authority label. A repo name, a transport digest, a workflow log or an internal `PROVED` flag is not enough.

# A. Resolved since the session-end reports

## Erdős #738 theorem-bank package — RESOLVED

A release report correctly recorded the exact five-file #738 theorem-bank package as unpublished at that session's endpoint. That state is now stale.

Current focused home:

`jaredwilder/erdos738-triangle-free-induced-trees`

The dedicated repository is populated with theorem/frontier banks, verifier material and focused source structure.

## Thirteen first-wave subject promotions — RESOLVED

Focused homes now exist and are populated for:

- Erdős–Gyárfás power-of-two cycles;
- Caccetta–Häggkvist directed triangles;
- fiber coherence / cycle rank / rank-three kernel;
- Erdős #890 ↔ #1093 divisor-window bridge;
- induced-P6 Erdős–Hajnal;
- Erdős #142 progression-free program;
- Erdős #1061 aliquot-square program;
- Erdős #271 Stanley sequences;
- Erdős #500 Turán `(3,4)`;
- Lonely Runner 13;
- Erdős #949 finite sums;
- Erdős #1066 unit distance;
- 41-vertex `R(5,5)` circulant structure.

## DNA constant-GC / reverse-complement program — RESOLVED AS SUBJECT HOME

Focused home:

`jaredwilder/constant-gc-reverse-complement-codes`

The public home preserves the exact `(7,5,3)=11` line and the 28-vs-29 frontier while explicitly retaining deeper certificate-byte recovery debt.

## Rado-equation avoidance atlas — RESOLVED AS SUBJECT HOME / DATA RECOVERY STILL OPEN

Focused home:

`jaredwilder/rado-equation-avoidance-atlas`

The 11-sequence / 839-value program is no longer buried solely inside `relation-family-atlas`. The original full machine-readable table bytes still require recovery.

## C(13,6,3) focused-home consolidation — RESOLVED

Focused home:

`jaredwilder/covering-13-6-3`

The stronger September structural packet, source scripts and human local-degree 8/9 eliminations have now been migrated into the focused repository. The parent exact value remains open at `20 <= C(13,6,3) <= 21`.

## Erdős #902 non-static receipts — RESOLVED

Static records now exist for:

- compact layerwise `f(4)>=49` contradiction architecture;
- McKay DRT(23) catalogue replay receipt.

The lower-bound candidate still exposes its external-classification dependency rather than hiding it.

## Transcript-only standalone mathematics — PARTLY RESOLVED

New subject records now preserve at least:

- uniform periodicity theorem for greedy sum-avoiding sequences;
- cyclic extremal `R(4,5)` witness on `Z_24` with deterministic verifier;
- `R^3`/`R^4` unit-distance density threshold note.

# B. Exact bytes known to exist but still not branch-visible

## 1,428-row contradiction history — OPEN BYTE RECOVERY

Object:

`08-CONTRADICTION-HISTORY-1428.csv`

- bytes: `1,878,201`;
- SHA-256:

`11cbb415bd54adc962d69969217ad5ebc94dec1b3ca8a1a64fd12cd8d8eaf8a6`.

A recovery manifest is now public under `msl-ore-estate/catalog/contradiction-history-1428/`, but the exact CSV bytes still need transport.

## ORE-v1 canonical payload — OPEN BYTE RECOVERY

Known identities:

`ore.jsonl`

`d45300265dc619701467fcd0a1046cc3f3e61a25359cbda37a3890c9180b2dbd`

`edges.jsonl`

`44472dc8ecac3fe36a08027612a3cbc22d11ceb5c0f95bc4988f256f59c38f43`

Public metadata records 458,212 objects / 1,133,352 edges; the giant payload bytes are not yet public.

## Readable local-PROVED 617 rows — OPEN PRESENTATION DEBT

Exact compressed transport is public and reconstructs a 617-row CSV with SHA-256

`4999693a7ad661344f8a05ae1c9dc00c9e2be91a6d1f9c842492e5b23047523a`.

The ordinary 13 readable CSV chunks are still not branch-visible. These are local campaign statuses, not 617 theorem certifications.

## Erdős #1061 primitive-seed certificate — OPEN BYTE RECOVERY

`ERDOS1061_PRIMITIVE_SEEDS_200K.csv`

- bytes: `17,026,297`;
- SHA-256:

`343b12fceb642d15b898f1a4bbbd9018b4a4301c0e72ac46ba30f559358e1a68`.

A recovery manifest is now public in the focused #1061 repository.

## Expanded pure-math archive — OPEN BYTE RECOVERY / NOT A THEOREM HOME

`GPT-MINE-EXPANDED-PURE-MATH-ESTATE.zip`

- about 40,213 files;
- about 722 MB;
- SHA-256:

`6e8e07a071d35cd3ae2bcb42a870ba2d0315f5bdfa7a9b9ee7a8ca4b98af9c38`.

This belongs in provenance/storage, not as a substitute for subject routing.

# C. Source assets not recovered to publication authority

## Exact 154-value C_k atlas — OPEN SOURCE RECOVERY

The structural program is public at `pascal-relation-extremal-atlas`; the exact original 154-value machine-readable atlas is missing. A recovery record now exists there.

## Full EG203 historical June package — OPEN FORENSIC SOURCE RELEASE

Known local archive identity from the release report:

- `EG203-HISTORICAL-PROOF-PACKAGE-2026-06-03.zip`;
- 1,365,278 bytes;
- SHA-256 `26e8891292a9efdf4eaa8e8f69ee9f9d1c6570233011f05dc68b33663f58f855`;
- 144 files / 125 Lean files.

Only the authority-critical core is public. Any full release must preserve unconditional, bounded, conditional, circular/deprecated, witness and manifest layers separately.

## Canonical 56 proof/authority source — OPEN SOURCE RECOVERY

The 56-statement/status surface is public. The exact source carrying proof/certificate paragraphs, receipt names, scope notes, corrections and blacklists is not confirmed public byte-for-byte.

## Canonical V2 registry / delta — OPEN SOURCE RECOVERY

The estate reports an 816-row canonical registry:

- 689 inherited theorem/claim records;
- 120 Weird-Round targets;
- 7 custom-kernel finite claims.

Authority mix reported locally: 536 proposed, 189 finite-checked, 68 proved-prose, 23 source-asserted/unclassified. The exact canonical source file is not confirmed public.

# D. Major semantic-review queues

## Pass-4 formalizer / proof / closure estate — OPEN

A live debt index is now public in `msl-ore-estate/PASS4-RELEASE-DEBT-2026-09-13.md`.

High-value unresolved accounting includes:

- 556 uncertified theorem/proof/witness-bearing formalizer objects;
- 1,488 unfinished/load-bearing ore objects;
- 448 exact duplicate theorem families;
- 336 near-duplicate theorem pairs;
- 606 live theorem-grade identities versus 238 promoted/curated rows at the recovered snapshot.

## Pass 7 — OPEN MAJOR SURFACE

Internal report: 752,539 objects, described at recovery time as never sealed / never touched. No blanket publication claim is made.

## Large ore store — OPEN MAJOR SURFACE

About 6.5 GB / 266,348 extracted files. This remains a mining source, not a theorem count.

# E. Reconstruct-first mathematical candidates

## Erdős #238 general small-c1 sibling — RECONSTRUCT FIRST

The release report records a deeper sieve result of the form

`B(x) <= C(c2) x / log^2 x`

leading to a positive `c_*(c2)` such that every fixed `0<c1<c_*(c2)` admits the stated long block of consecutive primes with pairwise separation exceeding `c2` for sufficiently large `x`.

The exact constant normalization/hypotheses and proof source must be reconstructed before promotion. Do not turn the report summary into a theorem by paraphrase.

## Power-of-three r_3 constructions — RECONSTRUCT FIRST

The estate references

- `r_3(243) >= 32`;
- `r_3(729) >= 64`;
- `r_3(2187) >= 128`.

The exact construction/proof bytes were not recovered to publication authority. Route into the focused #142 progression-free home only after exact witness/proof recovery.

## Entire-function card bank — UNRESOLVED PROOF GAP

`banked-with-gaps` preserves 17 entire-function statements whose historical cards say `PROVED-HERE` but contain no proof bodies. Leave as gap bank until reconstructed.

# F. New focused-home candidate awaiting shell

## Strongly-regular-graph transport atlas — PROMOTE

Currently buried in `mathematics-under-the-wrong-filename`:

- 119 strongly-regular parameter tuples;
- 91 existence / 28 nonexistence entries;
- 141 verified composable transport edges;
- eight transport types;
- 211 endpoints;
- explicit proposition-typing guard against invalid cross-object transports.

Proposed canonical slug:

`strongly-regular-graph-transport-atlas`

# G. IP-held / intentionally not automatic release

Do not bulk-publish mixed biomedical, patent, product-control, research-system or PeakProof architecture merely because mathematical fragments occur inside it. Extract separable pure mathematics only when doing so does not reveal load-bearing embodiment/IP.

# Saturation criterion

The estate is not saturated until every recoverable source family has one explicit disposition:

`CANONICAL SUBJECT HOME / EXISTING HOME / PROVENANCE / RECONSTRUCT FIRST / QUARANTINE / IP HOLD`.

The denominator is source identity, not repo count.
